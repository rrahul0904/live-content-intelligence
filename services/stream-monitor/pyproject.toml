[project]
name = "lci-stream-monitor"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["pydantic>=2.10,<3"]

[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"
